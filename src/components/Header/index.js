import React from 'react';
import {Query} from '@apollo/react-components';
import HEADER_QUERY from '../../queries/header/header'

const Header = () => {
    return ( 
        <>
            {/* <Query query={HEADER_QUERY}>
                {({data}) => {
                    console.log(data);
                }}
            </Query> */}
        </>
     );
}
 
export default Header;